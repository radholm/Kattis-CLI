let chalk,Listr;_093‍.x([["getProblem",()=>getProblem]]);_093‍.w("chalk",[["default",["chalk"],function(v){chalk=v}]]);_093‍.w("listr",[["default",["Listr"],function(v){Listr=v}]]);

const { exporting, Spider } = require("simple-webscraper");

       async function getProblem(options) {
  (async function () {
    try {
      const s = new Spider("https://open.kattis.com/problems");
      await s
        .appendSelector("table.problem_list > .tbody")
        .setExportFunct(exporting.file("pogchamp.log", "INFO %s, %s, %s"))
        .run();
    } catch (e) {
      _093‍.g.console.error(e);
    }
  })();

  //const tasks = new Listr([
  //    {
  //        title: 'Fetch Kattis problems',
  //        task: () => initProblems(options)
  //    },
  //]);

  //await tasks.run();

  _093‍.g.console.log("%s Problems ready", chalk.green.bold("DONE"));
}
